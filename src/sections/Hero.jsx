import React from "react";

const Hero = () => {
  return (
    <section
      id="home"
      className="w-full flex xl:flex-row flex-col min-h-screen max-container justify-center gap-10 "
    ></section>
  );
};

export default Hero;
